# steve0v0 个人小程序 PRD

> 版本：v0.4 ｜ 日期：2026-08-11 ｜ 状态：已确认  
> 技术栈：Spring Boot + MySQL + 小程序前端  
> 说明：本 PRD 聚焦各页面功能定义，UI 风格暂不定。

---

## 1. 项目概述

打造一个属于 steve0v0 的个人小程序，包含四个底部 Tab：首页、博客、动态、关于。公开访客无需登录即可浏览所有内容；管理员通过"长按关于页头像 5 秒 → 输入暗号"的隐藏入口进入后台管理，进行文章、动态、学习记录、课程表、个人资料和 GitHub 项目等内容的增删改查。

---

## 2. 页面功能

### 2.1 首页

核心定位：个人日常学习中心 + 状态看板。

#### 2.1.1 精美日历
- 默认展示当月日历，支持切换为周视图。
- 日期格子上可展示当天关联内容：
  - 学习记录（文字摘要）。
  - 导入后的课程表事件（课程名称）。
- 点击某一天，进入"当天详情"页面/弹窗，展示该日所有学习记录和课程安排。

#### 2.1.2 学习记录
- 日历上点击某天，展示当天的学习记录（管理员输入暗号进入后台后，可添加/编辑/删除学习记录）。
- 一条学习记录包含：学习主题、学习内容描述、学习时长、创建时间。
- 支持按日期查看历史学习记录。

#### 2.1.3 课程表导入
- 支持导入课程表并展示在日历上。
- 导入方式：
  - 上传课程表图片，由 AI 识别解析出课程信息（课程名、上课时间、地点）。
- 解析后的课程信息存入数据库，按周重复展示在对应日期上。

#### 2.1.4 番茄钟
- 倒计时功能：默认 25 分钟，可自定义时长。
- 可关联当前学习任务（自由文本，不强制关联学习记录）。
- 记录每日专注次数与总专注时长，供个人统计查看。
- 未登录用户可正常使用前端倒计时，但不会持久化保存。
- 登录用户完成倒计时后，提交记录到服务端持久化，需要 JWT 鉴权。
- 只保存倒计时设定时长（`duration`）和完成时间（`completed_at`），不记录开始/结束时间。
- 不支持手动补录未完成的番茄钟。

#### 2.1.5 个人状态
- 展示当前个人状态，例如：
  - 在线 / 学习中 / 忙碌 / 休息。
  - 当前正在做的事。
  - 心情签名。
- 管理员可在后台修改当前状态，前端实时展示。

#### 2.1.6 学习数据看板
- 展示与学习记录、番茄钟联动的统计数据：
  - 今日学习时长、今日完成番茄数。
  - 本周累计学习时长、本周番茄数趋势。
  - 连续学习天数。
  - 学习热力图：以日历格子颜色深浅展示近期学习频率（类似 GitHub 贡献图）。
- 数据来源：学习记录表 + 番茄钟记录表。
- 管理员无需手动维护，系统自动统计生成。

---

### 2.2 博客页

核心定位：技术文章与生活文章的分开展示。

#### 2.2.1 Tab 切换
- 顶部两个 Tab：
  - **技术博客**：技术教程、项目复盘、踩坑记录等。
  - **生活文章**：读书笔记、随笔、生活感悟等。

#### 2.2.2 文章列表
- 每个 Tab 下展示对应分类的文章列表。
- 列表项包含：
  - 发布日期
  - 预计阅读时长
  - 文章标题
  - 文章摘要
  - 标签（如：静态博客、GitHub Pages、全栈开发、AI 辅助开发等）
- 支持下拉刷新、上拉加载更多。

#### 2.2.3 文章详情
- 点击文章进入详情页，展示完整正文内容。
- 正文使用 Markdown 格式存储，前端自动渲染为 HTML 展示。
- 文章详情页展示：标题、发布时间、分类、标签、阅读数、正文内容。
- 支持 Markdown 中的标题、列表、代码块、图片、链接等常用语法。

---

### 2.3 动态页

核心定位：生活动态时间流，仅展示，无互动。

#### 2.3.1 动态流
- 按时间倒序展示生活动态。
- 每条动态包含：
  - 发布日期
  - 动态内容（纯文字/多图/音乐卡片/视频卡片等）
  - 相关图片或媒体资源
- 支持下拉刷新、上拉加载更多。

#### 2.3.2 内容形式
- 支持纯文字动态。
- 支持多图动态。
- 支持音乐/视频卡片（外链或上传资源）。

---

### 2.4 关于页

核心定位：个人资料展示 + GitHub 项目展示。技术栈和联系方式由前端静态维护，本阶段不做后端动态管理。

#### 2.4.1 个人资料
- 展示个人姓名和头像。
- 姓名和头像由后端配置，头像只保存外部 HTTPS 图片地址，不上传或保存图片文件。
- 数据库没有资料时，公开接口返回空姓名和空头像地址，由前端决定占位展示。

#### 2.4.2 开源项目
- 列表展示个人 GitHub 项目，所有已保存项目默认公开。
- 每个项目包含：项目名称、项目简介、GitHub 仓库地址、技术标签和展示排序。
- 项目地址只允许 GitHub HTTPS 地址，点击项目卡片跳转对应 GitHub 仓库。

#### 2.4.3 技术栈与联系方式
- 技术栈和联系方式由前端写死，不进入第三阶段数据库，不提供后端查询或管理接口。
- 联系方式的复制、跳转、二维码展示等交互留待后续前端阶段设计。

#### 2.4.4 隐藏管理入口
- 长按头像 5 秒，弹出暗号输入框。
- 输入正确暗号后进入后台管理页面。
- 暗号错误仅提示"暗号错误"，不暴露后台存在。

---

## 3. 管理后台功能

通过"关于页长按头像 → 输入暗号"进入后台，仅管理员可用。

### 3.1 暗号登录
- 前端长按头像触发暗号输入框。
- 后端校验暗号，校验通过后返回 JWT。
- 管理后台接口统一 JWT 鉴权。

### 3.2 博客管理
- 创建、保存草稿、发布和删除技术博客与生活文章，支持在草稿箱中查看草稿；已发布文章暂不支持修改。
- 文章字段：标题、摘要、正文（Markdown）、分类、标签、封面图、状态（草稿/已发布）、阅读数。
- 支持上传 UTF-8 编码的 `.md` / `.markdown` 文件；后端完成类型、大小和编码校验后返回原始正文，管理端自动填入编辑器并实时渲染预览。
- Markdown 中的图片处理方案：使用 PicGo 图床，写作时先将图片上传至图床获取网络 URL，Markdown 中只保留网络图片地址，前端直接渲染无需后端额外处理。

### 3.3 动态管理
- 发布和删除生活动态，支持纯文字和多图；暂不支持动态修改。
- 动态字段：内容、图片/媒体资源、发布时间。

### 3.4 学习记录管理
- 管理员在后台添加/编辑/删除学习记录。
- 学习记录字段：日期、主题、内容、学习时长。
- 保存后自动展示在首页日历对应日期上。

### 3.5 课程表管理
- 支持手动录入课程信息（AI 识别导入延后实现）。
- 课程表图片 AI 识别不属于当前阶段，安排在第四阶段。
- 课程字段：课程名、开始日期、结束日期、上课时间、下课时间、上课地点、星期几、是否每周重复。
- 课程模式：
  - 仅当天：`start_date = end_date`，不需要填写星期。
  - 每周重复：需填写星期（1-7），在 `start_date` 到 `end_date` 范围内按星期重复。

### 3.6 个人状态管理
- 修改当前个人状态（在线/学习中/忙碌/休息）、当前在做的事、心情签名。

### 3.7 关于页信息管理
- 管理个人姓名和头像，使用单例资料模型；通过 PUT 覆盖或清空，不提供资料删除接口。
- 管理 GitHub 项目，支持列表、详情、新增、修改和删除。
- 技术栈与联系方式不进入后端管理，暂由前端静态维护。

---

## 4. 数据模型概要

| 表名 | 说明 | 核心字段 |
|---|---|---|
| `article` | 文章表 | id, title, summary, content, category, tags, cover_image, status, view_count, created_at, updated_at |
| `moment` | 动态表 | id, content, images, media_type, created_at, updated_at |
| `study_record` | 学习记录表 | id, record_date, subject, content, duration, created_at, updated_at |
| `course` | 课程表 | id, name, start_date, end_date, start_time, end_time, location, day_of_week, is_repeated, created_at, updated_at |
| `personal_status` | 个人状态表 | id, state, current_task, mood, updated_at |
| `pomodoro` | 番茄钟记录表 | id, task_name, duration, completed_at, created_at |
| `about_profile` | 关于页单例资料表 | id, name, avatar_url, created_at, updated_at |
| `about_project` | GitHub 项目表 | id, name, description, github_url, tech_tags, sort, created_at, updated_at |

---

## 5. API 设计概要

### 5.1 公开接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/home/calendar` | 获取日历数据（学习记录 + 课程） |
| GET | `/api/home/status` | 获取当前个人状态 |
| GET | `/api/home/stats` | 获取学习数据看板统计 |
| GET | `/api/articles` | 文章列表（支持分类、分页） |
| GET | `/api/articles/{id}` | 文章详情 |
| GET | `/api/moments` | 动态流列表（分页倒序） |
| GET | `/api/about/profile` | 个人姓名和头像 |
| GET | `/api/about/projects` | 开源项目列表 |

> 番茄钟提交接口 `POST /api/home/pomodoro` 需要 JWT 鉴权，未登录用户仅使用前端本地倒计时，完成后不调用持久化接口。详见管理接口。

### 5.2 管理接口

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/admin/auth` | 无 | 暗号校验，返回 JWT |
| GET | `/api/admin/articles` | JWT | 管理端文章列表（含草稿） |
| GET | `/api/admin/articles/{id}` | JWT | 管理端文章详情（含草稿） |
| POST | `/api/admin/articles` | JWT | 创建文章、直接发布或保存为草稿 |
| DELETE | `/api/admin/articles/{id}` | JWT | 删除文章 |
| GET | `/api/admin/moments` | JWT | 管理端动态列表 |
| POST | `/api/admin/moments` | JWT | 发布动态 |
| DELETE | `/api/admin/moments/{id}` | JWT | 删除动态 |
| GET | `/api/admin/study-records` | JWT | 学习记录列表（支持日期范围筛选） |
| GET | `/api/admin/study-records/{id}` | JWT | 学习记录详情 |
| POST | `/api/admin/study-records` | JWT | 添加学习记录 |
| PUT | `/api/admin/study-records/{id}` | JWT | 编辑学习记录 |
| DELETE | `/api/admin/study-records/{id}` | JWT | 删除学习记录 |
| GET | `/api/admin/courses` | JWT | 课程列表 |
| GET | `/api/admin/courses/{id}` | JWT | 课程详情 |
| POST | `/api/admin/courses` | JWT | 保存课程信息 |
| PUT | `/api/admin/courses/{id}` | JWT | 编辑课程 |
| DELETE | `/api/admin/courses/{id}` | JWT | 删除课程 |
| POST | `/api/home/pomodoro` | JWT | 保存已完成番茄钟记录（登录用户完成后提交） |
| PUT | `/api/admin/status` | JWT | 更新个人状态 |
| GET | `/api/admin/about/profile` | JWT | 查询关于页个人资料 |
| PUT | `/api/admin/about/profile` | JWT | 覆盖或清空关于页个人资料 |
| GET | `/api/admin/about/projects` | JWT | GitHub 项目列表 |
| GET | `/api/admin/about/projects/{id}` | JWT | GitHub 项目详情 |
| POST | `/api/admin/about/projects` | JWT | 新增 GitHub 项目 |
| PUT | `/api/admin/about/projects/{id}` | JWT | 修改 GitHub 项目 |
| DELETE | `/api/admin/about/projects/{id}` | JWT | 删除 GitHub 项目 |
| POST | `/api/admin/upload` | JWT | 图片上传 |
| POST | `/api/admin/upload/markdown` | JWT | 导入 Markdown 文件并返回 UTF-8 正文 |

> 管理接口统一 JWT 鉴权，除 `/api/admin/auth` 外。番茄钟接口 `POST /api/home/pomodoro` 虽然路径在 `/api/home/` 下，但需要 JWT 鉴权，未登录用户仅使用前端本地倒计时。上传 Markdown 文件时，后端返回文件原始内容，前端填充到文章正文编辑框。已发布文章和动态暂不提供编辑接口，后续如需修改再单独设计。

---

## 6. 非功能需求

- 后端使用 Spring Boot + MySQL 实现。
- 响应格式统一为 `{ code, message, data }`。
- 管理接口统一鉴权，异常统一处理。
- 图片上传限制文件类型与大小。
- AI 课程表识别建议调用第三方 OCR/大模型 API，结果支持人工校对。

---

## 7. 本次范围说明

### 7.1 当前阶段范围
- 第一阶段：项目骨架、暗号登录、JWT 鉴权、博客、动态和图片上传。
- 第二阶段：首页日历、学习记录、手动课程、番茄钟、学习数据统计和个人状态。
- 第三阶段：关于页个人姓名、头像和 GitHub 项目动态管理；技术栈与联系方式由前端静态维护。
- 第四阶段：Markdown 文件上传解析已实现；课程表 AI 识别导入待实现。
- 博客页技术博客与生活文章 Tab 切换、列表、详情。
- 动态页生活动态流展示。
- 关于页隐藏管理入口。
- 后台管理：暗号登录、文章、动态、学习记录、课程表、个人状态、个人资料和 GitHub 项目管理。

### 7.2 本期不包含
- 访客注册/登录体系。
- 动态点赞、评论等互动功能。
- 富文本编辑器，文章正文使用 Markdown。
- 部署上线、HTTPS、SEO 优化。
- 具体 UI 视觉风格设计。
- 技术栈和联系方式的后端动态管理。
- 课程表 AI 识别仍待后续实现；Markdown 文件上传解析已完成。

---

## 8. 已确认决策

| 问题 | 决策 |
|---|---|
| 课程表 AI 识别调用哪个服务？ | 第四阶段再确定服务，本阶段不实现 |
| 番茄钟数据是否持久化？ | 持久化存储 |
| 文章详情是否需要在后台支持预览？ | 不需要 |
| 暗号是否支持修改？ | 支持修改 |
| 首页日历默认展示当月还是当周？ | 当月 |
| 番茄钟是否需要后台管理查看历史统计？ | 仅前端展示，不提供后台管理接口 |
| 课程表导入 AI 识别延后，是否先做手动录入？ | 是，先做手动录入 |
| 课程表 AI 识别安排在哪个阶段？ | 第四阶段，第二阶段和第三阶段不实现 |
| 番茄钟提交接口是否公开？ | 需要 JWT 鉴权，未登录用户仅使用前端本地倒计时，防止匿名刷统计数据 |
| 番茄钟是否记录开始/结束时间？ | 不记录，只保留 `duration`（设定时长）和 `completed_at`（完成时间），`duration` 是专注时长的唯一真源 |
| 番茄钟是否支持手动补录？ | 不支持，只保存倒计时完成记录 |
| 课程是否支持日期范围？ | 是，课程表新增 `start_date`/`end_date` 字段，每周重复课程在日期范围内按星期生成 |
| 关于页哪些内容由后端动态管理？ | 只管理姓名、头像和 GitHub 项目；技术栈与联系方式由前端写死 |
| 关于页姓名和头像如何存储？ | 使用 `about_profile` 单例记录，头像保存外部 HTTPS URL，PUT 支持覆盖或清空，不提供 DELETE |
| GitHub 项目需要哪些能力？ | 使用 `about_project` 表，支持标准 CRUD，所有项目默认公开 |
| GitHub 项目技术标签如何存储？ | 接口使用 JSON 数组，数据库使用 MySQL JSON 字段 |
| 联系方式是否在本阶段提供跳转或复制？ | 暂不设计，留待后续前端阶段 |

---

## 9. 待确认问题

无。所有问题已确认，详见第 8 节已确认决策。
